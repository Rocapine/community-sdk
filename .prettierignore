# Lockfile — not hand-formatted, huge, and npm owns its shape.
package-lock.json

# Build output — generated by tsc, not source.
lib/

# Copied at CLI prebuild time from supabase/migrations + supabase/functions
# (the real source); reformatting the copies directly would just be undone
# on the next `npm run build -w @rocapine/community`.
templates/

# Expo tooling cache.
.expo/

node_modules/
supabase/.temp/
