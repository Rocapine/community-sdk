# Copy this to .env and fill in a Supabase project's URL + anon key to run
# this demo against a live backend. Leave both unset (or delete .env) to run
# in degraded mode: the feed renders its empty state, one console warning is
# logged, and nothing crashes.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_KEY=
